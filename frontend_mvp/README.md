# Taskr Script Frontend MVP (deprecated)

> This shell-script frontend was an early MVP. It is no longer maintained.
> Use the React + Vite + TypeScript frontend in `../Frontend` instead.
>
> Some scripts in `scripts/` may still work for ad-hoc testing, but the question-answering flow they assume has been removed from the backend.
